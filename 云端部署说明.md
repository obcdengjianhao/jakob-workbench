# 配方研发工作台云端版部署说明

这个文件夹是一个完整的云端同步版应用，包含：

- 电脑端工作台：`/workbench.html`
- 手机端工作台：`/mobile-workbench-app/`
- 统一同步接口：`/api/state`
- 健康检查接口：`/api/health`
- 初始数据：`data/workbench-state.json`

## 功能

- 手机和电脑访问同一个云端网址。
- 待办、日程提醒、项目清单、阶段、状态、风险信息共用一份云端数据。
- 手机端修改后，电脑端刷新或自动同步后可见。
- 电脑端修改后，手机端重新打开或回到页面后可见。
- 支持 `APP_PASSWORD` 环境变量设置访问密码。

## 本地测试

在这个文件夹中运行：

```bash
npm install
npm start
```

打开：

```text
http://localhost:3000/mobile-workbench-app/
http://localhost:3000/workbench.html
```

如果设置访问密码：

```bash
set APP_PASSWORD=你的密码
npm start
```

## 推荐部署方式

### 方案一：Render

1. 把 `cloud-workbench-app` 上传到 GitHub 仓库。
2. 登录 Render，选择 `New Web Service`。
3. 连接 GitHub 仓库。
4. Build Command 填：

```bash
npm install
```

5. Start Command 填：

```bash
npm start
```

6. 添加环境变量：

```text
APP_PASSWORD=你的访问密码
DATA_DIR=/var/data
```

7. 如果需要长期保存数据，给服务添加 Persistent Disk，并挂载到：

```text
/var/data
```

部署完成后访问：

```text
https://你的域名/mobile-workbench-app/
https://你的域名/workbench.html
```

### 方案二：Railway

1. 把 `cloud-workbench-app` 上传到 GitHub 仓库。
2. 登录 Railway，新建项目并连接该仓库。
3. 添加环境变量：

```text
APP_PASSWORD=你的访问密码
DATA_DIR=/app/data
```

4. 如需长期保存数据，请启用 Railway Volume，并把 `DATA_DIR` 指向挂载路径。

### 方案三：自己的服务器

服务器安装 Node.js 18+ 后：

```bash
npm install
APP_PASSWORD=你的访问密码 DATA_DIR=./data npm start
```

建议再用 Nginx 配置 HTTPS 域名。

## 注意事项

- 如果没有设置 `APP_PASSWORD`，任何知道网址的人都可以读写工作台，不建议公网使用。
- 如果部署平台没有持久化磁盘，服务重启后数据可能回到初始状态。
- 长期稳定使用建议选择带持久化磁盘的部署方式，或后续升级为数据库版。

